import { Router } from "express";

const route = Router();

// const routersModule = [
//     {
//         path :'/',
//         // route: 
//     },
// ];

// routersModule.forEach((r) => {
    // route.use(r.path, r.route);
// });

export default route;